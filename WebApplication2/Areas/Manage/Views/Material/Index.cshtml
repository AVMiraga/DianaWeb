@model List<Material>

@{
    int count = 0;
}

<div class="table-responsive pt-3 mx-auto">
    <h2 class="text-center">Material List</h2>
    <a class="btn btn-primary" asp-action="CreateMaterial">Create Material</a>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>
                    #
                </th>
                <th>
                    Name
                </th>
                <th>
                    Action
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                count++;
                <tr>
                    <td>
                        @count
                    </td>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        <a class="btn btn-danger" asp-action="DeleteMaterial" asp-route-id="@item.Id">Delete</a>
                        <a class="btn btn-warning" asp-action="UpdateMaterial" asp-route-id="@item.Id">Update</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>